---
# Varaibles that a user may want to motify

# Required variables
buildkite_agent_token: __agent_token_here__

# Optional variables
buildkite_agent_debug: false # set true for debug
buildkite_agent_tags: "ci=true,docker=true"
buildkite_agent_release: 3.32.0

buildkite_agent_private_ssh_key_path: "{{ playbook_dir }}/../../files/id_rsa"
buildkite_agent_public_ssh_key_path: "{{ playbook_dir }}/../../files/id_rsa.pub"
